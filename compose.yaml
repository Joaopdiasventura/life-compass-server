services:
  server:
    build:
      context: .
      target: final
    ports:
      - 8080:8080
    depends_on:
      - db
  db:
    image: mongo:8.0
    volumes:
      - life-compass-data:/data/db
    ports:
      - '27017:27017'

volumes:
  life-compass-data: